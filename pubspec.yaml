name: ecclesia_next
description: App White-Label para igrejas (SaaS).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- Gerência de Estado ---
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  dartz: ^0.10.1

  # --- Injeção de Dependência ---
  get_it: ^9.2.0


  
  # --- Supabase ---
  supabase_flutter: ^1.10.25 # Using a stable version compatible with older SDK constraints if needed, or 2.0.0 if safe. User asked for ^2.0.0.
  url_strategy: ^0.2.0

  # --- UI & Design ---
  cupertino_icons: ^1.0.2
  google_fonts: ^6.2.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/config/
    # Se tiver imagens, garanta que a pasta existe ou comente a linha abaixo
    # - assets/images/